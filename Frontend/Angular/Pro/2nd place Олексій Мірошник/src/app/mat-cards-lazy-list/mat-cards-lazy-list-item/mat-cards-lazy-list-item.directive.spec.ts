import {MatCardsLazyListItemDirective} from './mat-cards-lazy-list-item.directive';

describe('MatCardsLazyListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new MatCardsLazyListItemDirective();
    expect(directive).toBeTruthy();
  });
});
