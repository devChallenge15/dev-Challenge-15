import {MatCardLazyListScrollUpDirective} from './mat-card-lazy-list-scroll-up.directive';

describe('MatCardLazyListScrollUpDirective', () => {
  it('should create an instance', () => {
    const directive = new MatCardLazyListScrollUpDirective();
    expect(directive).toBeTruthy();
  });
});
